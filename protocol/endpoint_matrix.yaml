schema_version: 1
updated: "2026-07-31"
canonical_client_boundary: "Chapter 2-1 clear"
statuses:
  confirmed: "wire shape and surviving-client acceptance are confirmed"
  static_confirmed: "client-static contract is confirmed; live acceptance is incomplete"
  local_policy: "implemented preservation behavior, not a historical-service claim"
  unsupported: "fails explicitly rather than returning generic success"
capabilities:
  - family: bootstrap
    routes: [get_current_time, get_server_status, signup, login, userdata, userdata_after_close]
    status: confirmed
    transport_tests: true
    restart_tests: true
  - family: tutorial_story
    routes: [do_slot, userdata, start_quest, clear_quest]
    status: confirmed
    note: "first kind=10 Pact uses the maintainer-supplied retail 50/50 Bahl-or-Grace rule with one-time durable selection and byte-stable restart replay; Grace is client-confirmed, original-client Bahl continuation is pending; post-Chapter-1-1 Tutorial03 restore remains phase-preserving and restart-tested"
    transport_tests: true
    restart_tests: true
  - family: ordinary_story_chapter_2_to_42
    routes: [start_quest, clear_quest, userdata]
    status: local_policy
    transport_tests: true
    restart_tests: true
  - family: hunting_metal_and_default_special
    routes: [start_quest, clear_quest, unlock_metal_zone]
    status: local_policy
    note: "Chapter 3003-1 accepts the Issue 25 final-client 1800-Coin result; full historical reward distribution remains unknown"
    transport_tests: true
    restart_tests: true
  - family: pacts_and_companions
    routes: [do_slot, do_buddy_slot, sell_buddy, sell_buddies, buddy_strengthen, buddy_evolve, userdata]
    status: local_policy
    note: "new Companion equip targets require APK-derived character-family and active-job species authorization; RequiredLevel is client-side effect activation"
    transport_tests: true
    restart_tests: true
  - family: progression_mutations
    routes: [continue, refill_stamina, use_statusup_item, add_job, rebirth, summon_skill_unlock]
    status: local_policy
    note: "summon_skill_unlock preserves a legacy static contract; Version 5.5.0 retired its UI and Eidolon battle use"
    transport_tests: true
    restart_tests: true
  - family: messages_achievements_trading
    routes: [achived, read_messages, delete_messages, get_current_exchange, exchange]
    status: local_policy
    transport_tests: true
    restart_tests: true
  - family: local_events
    routes: [start_quest, clear_quest]
    status: local_policy
    note: "guided setup derives Archive Chapters 2000, 2001, 2002, 2004, and 2006 plus bounded Tower 9100-1; bundled Strikes Back Chapters 8000-8007 remain authoritative; Jade Dragon 2004-1 clear and restart persistence are original-client confirmed, while other event clears remain pending"
    transport_tests: true
    restart_tests: true
  - family: tower_vertical_slice
    routes: [get_server_status, login, start_quest, clear_quest]
    status: local_policy
    note: "only 9100-1 is projected after Chapter 3; 9100 mapping is strongly inferred from final-client static and BattleData evidence; original-client navigation and clear are pending"
    transport_tests: true
    restart_tests: true
  - family: multiplayer_social_commerce
    routes: [multiplayer, matchmaking, friends, purchases, advertisements]
    status: unsupported
    transport_tests: false
    restart_tests: false
